import { Theme } from '@emotion/react';

import { colors } from '@/styles/color';
import { fontSize, fontWeight } from '@/styles/font';
import * as scale from '@/styles/scale';

const theme: Theme = {
	fontSize,
	fontWeight,
	colors,
	scale,
};
export default theme;
